services:

  proxy:
    image: nginx:latest
    container_name: proxy-cliente3
    restart: unless-stopped
    volumes:
      - ./proxy/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - isp_net
      - cliente3_net

  portal:
    build:
      context: ./portal
    container_name: portal-cliente3
    restart: unless-stopped
    networks:
      - cliente3_net

  hotsite:
    build:
      context: ./hotsite
    container_name: hotsite-cliente3
    restart: unless-stopped
    networks:
      - cliente3_net

  sign-db:
    image: postgres:15-alpine
    container_name: sign-db-cliente3
    restart: unless-stopped
    environment:
      POSTGRES_USER: dbuser
      POSTGRES_PASSWORD: dbuserpass123
      POSTGRES_DB: docsign_db
    volumes:
      - ./sign/postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dbuser -d docsign_db"]
      interval: 5s
      timeout: 5s
      retries: 5

    networks:
      - cliente3_net

  sign-api:
    image: salesfilho/sign-api:latest
    container_name: sign-api-cliente3
    restart: unless-stopped

    environment:
      DB_HOST: sign-db-cliente3
      DB_PORT: 5432
      DB_USER: dbuser
      DB_PASSWORD: dbuserpass123
      DB_NAME: docsign_db

    depends_on:
      sign-db:
        condition: service_healthy

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

    networks:
      - cliente3_net

  sign-app:
    image: salesfilho/sign-app:latest
    container_name: sign-app-cliente3
    restart: unless-stopped

    environment:
      API_BASE_URL: http://sign-api-cliente3:8000/api

    depends_on:
      sign-api:
        condition: service_healthy

    networks:
      - cliente3_net

networks:

  isp_net:
    external: true

  cliente3_net:
    driver: bridge